#include <stdio.h>
#include <string.h>
int main(){
    // char str[100];
    // fgets(str,sizeof(str),stdin);
    // char* p=strchr(str,'\n');
    // *p='\0';
    // printf("%d\n",strlen(str));
    // return 0;
    printf("I am happy");
}